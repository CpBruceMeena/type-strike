package com.typestrike.ui.theme

import androidx.compose.ui.graphics.Color

// ── Backgrounds ──────────────────────────────────────────
val Background = Color(0xFF0A0A10)
val Surface = Color(0xFF1A1A28)
val SurfaceBorder = Color(0xFF2A2A3A)
val SurfaceDark = Color(0xFF141420)
val SurfaceCard = Color(0xFF12121E)

// ── Primary / Accent ─────────────────────────────────────
val MagmaRed = Color(0xFFFF5020)
val MagmaRedDark = Color(0xFFCC3300)
val MoltenGold = Color(0xFFFFCC00)
val NeonPurple = Color(0xFFCC44FF)

// ── Text ─────────────────────────────────────────────────
val TextWhite = Color(0xFFFFFFFF)
val TextBody = Color(0xFFCCCCCC)
val TextLabel = Color(0xFF888888)
val TextMuted = Color(0xFF666666)
val TextDisabled = Color(0xFF555555)

// ── States ───────────────────────────────────────────────
val ErrorRed = Color(0xFFFF2200)
val SuccessGreen = Color(0xFF22FF44)
val ScrimBlack = Color(0x80000000)

// ── Activity Colors ──────────────────────────────────────
val ActivityCompleted = MagmaRed
val ActivityFailed = ErrorRed
val ActivityAchievement = NeonPurple
val ActivityLevelUp = MoltenGold
val ActivityHighScore = MoltenGold

// ── Tier Colors ──────────────────────────────────────────
val TierEmber = MagmaRed
val TierIgneous = Color(0xFFFF6600)
val TierMagmaCore = NeonPurple
val TierObsidian = Color(0xFFCCCCCC)
