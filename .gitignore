# ── Go Backend ──────────────────────────────────────────
backend/tmp/
backend/server
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
*.test.exe

# ── Android App (Kotlin/Jetpack Compose) — on hold ──
type-strike-android/

# ── General ────────────────────────────────────────────
*.swp
*.swo
*~
.DS_Store
Thumbs.db
*.log
*.pid
.env
.env.local
backend/data/
node_modules/
.code-review-graph/
.code_review_graph/
code-review-graph-cache/
crg-cache/
.cache/code-review-graph/
.cache/crg/
*.crg

# ── Cabinet (product docs, specs, design reviews) ────
cabinet/
type-strike-wireframe.html

# ── Root-level assets / screenshots ───────────────────
*.png
screenshot_*.png

# ── Draft / temp files ────────────────────────────
coder.html
index.html
temp/
website-*.zip
"typing screen.zip"
typing\ screen.zip

# ── Android Keystore (DO NOT COMMIT) ───────────────────
*.keystore
*.jks
*.key

# ── Release builds ────────────────────────────────────
*.apk
*.aab
release/

# ── Editor / OS ───────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.bak
*.bak
.idea/
.playwright-mcp/

# ── Backend build artifacts ──────────────────────────
backend/scripts/seed_levels
backend/server.log

# ── Database migrations (applied externally / auto-apply on start) ──
backend/migrations/*.sql
# ── Clerk auth migration (WIP — skip for now) ──────
backend/migrations/005_clerk_auth.sql

# ── Website (Next.js) ────────────────────────────────
type-strike-web/.next/
type-strike-web/out/

# ── Process & lock files ──────────────────────────────
*.pid
*.lock
server.pid
